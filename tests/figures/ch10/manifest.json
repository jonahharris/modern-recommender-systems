[
  {
    "number": "10.1",
    "title": "Template-based explanation heuristic",
    "file": "listing_10_1_template_based_explanation_heuristic.py",
    "source_lines": [
      84,
      90
    ],
    "compiles": true,
    "provides": [
      "generate_explanation_heuristic"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Pure function definition. References undefined helpers get_user_favorite_genres/get_item_genres only at call time, so the def itself executes cleanly in the shared namespace."
  },
  {
    "number": "10.3",
    "title": "Wrapping existing recommendations with LLM explanations",
    "file": "listing_10_3_wrapping_existing_recommendations_with_llm_explanations.py",
    "source_lines": [
      140,
      155
    ],
    "compiles": true,
    "provides": [
      "explain_recommendations"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Pure function definition; takes an llm object at call time and calls llm.generate(system_prompt=..., user_message=...), which matches the shipped recsys.agentic.llm_client.LLMClient.generate(system_prompt, user_message=None, messages=None) signature. No import drift (this listing imports nothing). Def executes cleanly. Duplicate listing number 10.3 (the other 10.3 is 'Unified retrieval')."
  },
  {
    "number": "10.3",
    "title": "Unified retrieval",
    "file": "listing_10_3_unified_retrieval.py",
    "source_lines": [
      169,
      199
    ],
    "compiles": true,
    "provides": [
      "HybridRetriever"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "CORRECTED: rewritten to match the shipped recsys.agentic.hybrid_retriever.HybridRetriever(faiss_index, embeddings, movies_df, item_knn=None, encoder_name='all-MiniLM-L6-v2') with search(query, user_id=None, k=10, filters=None) and _reciprocal_rank_fusion(rrf_k=60). Removed the phantom `from recsys.retrieval import ContentRetriever, CollaborativeRetriever` (that module/classes never existed). Compile-checked only: module-level `from sentence_transformers import SentenceTransformer` plus a FAISS index/embeddings are needed to run. Duplicate listing number 10.3 (the other 10.3 is 'Wrapping existing recommendations')."
  },
  {
    "number": "10.4",
    "title": "RAG recommendation chain",
    "file": "listing_10_4_rag_recommendation_chain.py",
    "source_lines": [
      223,
      233
    ],
    "compiles": true,
    "provides": [
      "rewrite_query"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Pure function definition; takes an llm object at call time. Def executes fine."
  },
  {
    "number": "10.5",
    "title": "HyDE for recommendation retrieval",
    "file": "listing_10_5_hyde_for_recommendation_retrieval.py",
    "source_lines": [
      252,
      263
    ],
    "compiles": true,
    "provides": [
      "hyde_retrieval"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Pure function definition; takes llm + retriever at call time. Def executes fine."
  },
  {
    "number": "10.6",
    "title": "RAG recommendation chain",
    "file": "listing_10_6_rag_recommendation_chain.py",
    "source_lines": [
      300,
      319
    ],
    "compiles": true,
    "provides": [
      "build_recommendation_prompt"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: `system_prompt` is now assigned inside the function (the RAG system-prompt prose the chapter previously showed only above the listing), so `return system_prompt, user_message` no longer raises NameError. Def executes fine; takes candidates/user_profile/conversation_history at call time."
  },
  {
    "number": "10.7",
    "title": "End-to-end RAG recommendation",
    "file": "listing_10_7_end_to_end_rag_recommendation.py",
    "source_lines": [
      328,
      340
    ],
    "compiles": true,
    "provides": [
      "recommend_with_rag"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: now calls `retriever.search(query, user_id=..., k=15)` to match the shipped HybridRetriever.search(query, user_id=None, k=10, filters=None) (was `hybrid_search`, which does not exist). Def executes fine; pairs with the corrected L10.6 build_recommendation_prompt."
  },
  {
    "number": "10.8",
    "title": "Fetching extended context for explanation grounding",
    "file": "listing_10_8_fetching_extended_context_for_explanation_grounding.py",
    "source_lines": [
      362,
      370
    ],
    "compiles": true,
    "provides": [
      "enrich_candidates"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Pure function definition; takes a document_store at call time. Def executes fine."
  },
  {
    "number": "10.10",
    "title": "Agent tools for movie recommendation",
    "file": "listing_10_10_agent_tools_for_movie_recommendation.py",
    "source_lines": [
      414,
      442
    ],
    "compiles": true,
    "provides": [
      "tools"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "Module-level list literal that binds `retriever.search` and `filter_by_metadata`, neither of which is defined in the harness namespace -> NameError if executed. Needs a live HybridRetriever instance and a filter helper. Compile-checked only."
  },
  {
    "number": "10.11",
    "title": "Minimal ReAct agent for movie recommendation",
    "file": "listing_10_11_minimal_react_agent_for_movie_recommendation.py",
    "source_lines": [
      455,
      568
    ],
    "compiles": true,
    "provides": [
      "MovieRecommenderAgent",
      "json",
      "re"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Self-contained class definition (imports json, re itself). Def executes fine. Reprints a SIMPLIFIED version of the shipped recsys.agentic.agent.MovieRecommenderAgent: book run(self, user_query, user_id=None) vs package run(self, user_query, user_id=None, debug=False); book only accepts dict-tools while package also accepts Tool objects; package adds LLM-error handling, implicit-final-answer detection, observation truncation, and get_trace(). Signatures compatible, behavior richer in the package."
  },
  {
    "number": "10.12",
    "title": "Agent trace for a multi-step recommendation",
    "file": "listing_10_12_agent_trace_for_a_multi_step_recommendation.py",
    "source_lines": [
      594,
      603
    ],
    "compiles": true,
    "provides": [
      "agent",
      "response"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "Usage snippet: instantiates MovieRecommenderAgent(llm=llm, tools=tools) and calls .run(); `llm` and `tools` are undefined in the harness namespace and a real run needs an LLM. Compile-checked only."
  },
  {
    "number": "10.14",
    "title": "A trace of an agentic rec execution",
    "file": "listing_10_14_a_trace_of_an_agentic_rec_execution.py",
    "source_lines": [
      609,
      621
    ],
    "compiles": false,
    "provides": [],
    "category": "output",
    "executable": false,
    "expected": "skip",
    "notes": "Not code: a rendered [thought]/[action]/[observation]/[final_answer] execution trace printed as a listing. Does not parse (SyntaxError: invalid decimal literal, line 6). Output-not-code, recorded via compiles=false."
  },
  {
    "number": "10.15",
    "title": "Generative model as an agent tool",
    "file": "listing_10_15_generative_model_as_an_agent_tool.py",
    "source_lines": [
      635,
      664
    ],
    "compiles": true,
    "provides": [
      "GenerativeRetrieverTool"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: removed the phantom `from recsys.generative import generate_recommendations, SemanticDecoder` (that module never existed under recsys; the Chapter 7/9 generative model lives in the chapter notebooks). The generative model, tokenizer, decoder, formatter, and generation function are now injected via __init__ (documented-placeholder pattern), so the class is self-contained and its def executes cleanly. Real recommendations still require the Chapter 7/9 components at call time."
  },
  {
    "number": "10.16",
    "title": "Adding a tool to the agent toolbox",
    "file": "listing_10_16_adding_a_tool_to_the_agent_toolbox.py",
    "source_lines": [
      678,
      690
    ],
    "compiles": true,
    "provides": [],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "Mutates `tools` (from L10.10) and binds `gen_tool.recommend_from_history` (from L10.15's GenerativeRetrieverTool); both undefined in the harness namespace -> NameError. Compile-checked only."
  },
  {
    "number": "10.17",
    "title": "Conversation memory with summarization",
    "file": "listing_10_17_conversation_memory_with_summarization.py",
    "source_lines": [
      718,
      755
    ],
    "compiles": true,
    "provides": [
      "ConversationMemory"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Self-contained class definition. Def executes fine and matches the shipped recsys.agentic.memory.ConversationMemory constructor/methods (add_turn, get_context, _compress) closely; the package adds a clear() method. Behavior verified with a fake LLM: threshold-triggered compression and sliding window work as printed."
  },
  {
    "number": "10.18",
    "title": "Natural-language user taste profiler",
    "file": "listing_10_18_natural_language_user_taste_profiler.py",
    "source_lines": [
      772,
      793
    ],
    "compiles": true,
    "provides": [
      "UserProfiler"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: aligned with shipped recsys.agentic.memory.UserProfiler — __init__(self, llm, profile_store=None) now defaults the store to {}, and get_profile/update_profile coerce keys with str(user_id). Self-contained class definition; def executes fine. (The package additionally provides save()/load().)"
  }
]