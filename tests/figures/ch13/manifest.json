[
  {
    "number": "15.1",
    "title": "A/B experiment",
    "file": "listing_15_1_a_b_experiment.py",
    "source_lines": [
      78,
      81
    ],
    "compiles": true,
    "provides": [],
    "category": "pseudocode",
    "executable": false,
    "expected": "skip",
    "notes": "CORRECTED: wrapped the loose if/else routing fragment in `def route_request()` and added `import random` so it parses. Still illustrative pseudocode: control()/variant() are undefined by design, so it is compile-checked only, not executed."
  },
  {
    "number": "15.2",
    "title": "Epsilon-greedy algorithm",
    "file": "listing_15_2_epsilon_greedy_algorithm.py",
    "source_lines": [
      146,
      152
    ],
    "compiles": true,
    "provides": [],
    "category": "pseudocode",
    "executable": false,
    "expected": "skip",
    "notes": "CORRECTED: the bare select-body fragment (which raised 'return outside function') is now shown inside `def select_arm(self, epsilon)` with consistent indentation, so it parses. Illustrative: num_arms / estimated_arm_values are undefined by design (the full class is L15.3), so compile-checked only."
  },
  {
    "number": "15.3",
    "title": "Epsilon-greedy bandit class",
    "file": "listing_15_3_epsilon_greedy_bandit_class.py",
    "source_lines": [
      164,
      194
    ],
    "compiles": true,
    "provides": [
      "EpsilonGreedyBandit"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "RECONSTRUCTED: the original figure was EMPTY because the class was typeset as un-fenced escaped text and captured nothing. Rebuilt the full EpsilonGreedyBandit class (init / select_arm / update_estimates) as a proper fenced listing. Uses only np (available in the harness ns), so it defines cleanly and provides EpsilonGreedyBandit for L15.5."
  },
  {
    "number": "15.4",
    "title": "Bandit Simulator parameters",
    "file": "listing_15_4_bandit_simulator_parameters.py",
    "source_lines": [
      218,
      228
    ],
    "compiles": true,
    "provides": [
      "cumulative_regrets",
      "epsilon_values",
      "num_arms",
      "num_trials",
      "optimal_arm_reward",
      "true_reward_probabilities"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED callouts: added the missing legend entries for #E and #F (marker-no-explanation). Pure literal assignments plus max(); runs to completion and provides the simulation parameters consumed by L15.5."
  },
  {
    "number": "15.5",
    "title": "Bandit Simulator",
    "file": "listing_15_5_bandit_simulator.py",
    "source_lines": [
      242,
      272
    ],
    "compiles": true,
    "provides": [
      "cumulative_regrets",
      "optimal_arm_reward"
    ],
    "category": "needs-prior",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED callouts: removed the duplicate #F legend entry (a leftover from the epsilon-greedy listing) and consolidated the wording (duplicate-callout). The listing itself was already correct; it now runs end-to-end because L15.3 (EpsilonGreedyBandit) is reconstructed and L15.4 provides the params, all present in the cumulative namespace before this listing runs."
  },
  {
    "number": "15.6",
    "title": "Epsilon-greedy algorithm with recommender system",
    "file": "listing_15_6_epsilon_greedy_algorithm_with_recommender_system.py",
    "source_lines": [
      330,
      336
    ],
    "compiles": true,
    "provides": [],
    "category": "pseudocode",
    "executable": false,
    "expected": "skip",
    "notes": "CORRECTED: wrapped the loose cold-start if/else fragment in `def select_recommendation(epsilon)` so it parses (was 'return outside function'). Illustrative: select_a_cold_start_item() / recsys_model are placeholders undefined by design, so compile-checked only."
  },
  {
    "number": "15.7",
    "title": "Bayesian bandit",
    "file": "listing_15_7_bayesian_bandit.py",
    "source_lines": [
      364,
      390
    ],
    "compiles": true,
    "provides": [
      "BayesianBandit"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: dedented `update_estimates` (was over-indented by one space -> IndentationError) and normalised the class to 4-space method bodies; added `from scipy.stats import beta` which `beta.rvs` needs. scipy is installed in the venv, so the class defines cleanly and provides BayesianBandit."
  },
  {
    "number": "15.8",
    "title": "An abstract bandit",
    "file": "listing_15_8_an_abstract_bandit.py",
    "source_lines": [
      456,
      470
    ],
    "compiles": true,
    "provides": [],
    "category": "pseudocode",
    "executable": false,
    "expected": "skip",
    "notes": "CORRECTED: `For`->`for`, converted tab-indented bodies to spaces, and fixed the spelling `availble_arms`->`available_arms`, so it parses. Also fixed callouts: removed the duplicate #D legend entry and gave the `np.argmax` line its own #E entry (duplicate-callout). Illustrative abstract base: arm.calc_value / context are undefined by design, so compile-checked only."
  },
  {
    "number": "15.9",
    "title": "UCB bandit",
    "file": "listing_15_9_ucb_bandit.py",
    "source_lines": [
      509,
      547
    ],
    "compiles": true,
    "provides": [
      "UCBAlgorithm"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: joined the dangling `/` operator into one line (`... ) / self.n_values[chosen_arm]`), fixed the `Self`->`self` typo, and indented `return arm` to 4 spaces inside select_arm (was at class-body level -> 'return outside function' and select_arm returned None). Also fixed callouts: added the missing #F legend and corrected #A wording (it was mislabeled as a Thompson-sampling bandit). Uses only np, defines and runs; provides UCBAlgorithm."
  },
  {
    "number": "15.10",
    "title": "LinUCBlinucb disjoint arm implementation",
    "file": "listing_15_10_linucblinucb_disjoint_arm_implementation.py",
    "source_lines": [
      635,
      665
    ],
    "compiles": true,
    "provides": [
      "linucb_disjoint_arm"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: normalised the mangled indentation (every method to a 2-space class body / 4-space method body; `__init__` was at 3 spaces vs calc_UCB/reward_update at 2, and inside calc_UCB `x`/`p`/`return` were at 7/14/7 spaces). Also added the missing #K legend entry (marker-no-explanation). Uses only np; defines and runs; provides linucb_disjoint_arm for L15.11."
  },
  {
    "number": "15.11",
    "title": "linucb disjoint policy implementation",
    "file": "listing_15_11_linucb_disjoint_policy_implementation.py",
    "source_lines": [
      693,
      727
    ],
    "compiles": true,
    "provides": [
      "linucb_policy"
    ],
    "category": "needs-prior",
    "executable": true,
    "expected": "pass",
    "notes": "CORRECTED: normalised indentation (`__init__` at 4 spaces vs `select_arm` at 2 -> IndentationError). Also fixed two algorithmic bugs: the tie-breaker `if arm_ucb == highest_ucb` (nested inside the `>` block, so it only duplicated the current arm) is now a sibling `elif`, and each arm is built with `arm_index=i` instead of the hardcoded `arm_index=1`. Depends on linucb_disjoint_arm (L15.10); the listing defines the class (no instantiation at module level) so it runs after L15.10 is present."
  }
]
