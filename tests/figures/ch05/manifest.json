[
  {
    "number": "5.1",
    "title": "Creating positive pairs from user interaction histories",
    "file": "listing_5_1_creating_positive_pairs_from_user_interaction_histories.py",
    "source_lines": [
      53,
      62
    ],
    "compiles": true,
    "provides": [
      "create_pairs_for_user"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Pure function definition over a pandas group. No imports needed at def time; verified it execs clean in the harness namespace."
  },
  {
    "number": "5.2",
    "title": "Creating negative pairs by random sampling",
    "file": "listing_5_2_creating_negative_pairs_by_random_sampling.py",
    "source_lines": [
      75,
      89
    ],
    "compiles": true,
    "provides": [
      "create_negative_pairs"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Function definition execs clean (annotations are builtins). Latent runtime deps: references module-level `random` (never imported in the listing) and `movie_to_idx` only when called. Latent BOOK BUG: appends `(movie1, movie_to_idx[movie3])` \u2014 the negative pair mixes a movie ID with an index, inconsistent with the positive pairs which are (movieId, movieId)."
  },
  {
    "number": "5.3",
    "title": "The two-tower model",
    "file": "listing_5_3_the_two_tower_model.py",
    "source_lines": [
      121,
      154
    ],
    "compiles": true,
    "provides": [
      "TwoTower"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "`class TwoTower(nn.Module)` needs torch (`nn`, `torch`) at class-definition time; not in the harness namespace -> NameError. Runs only in a torch session."
  },
  {
    "number": "5.4",
    "title": "Building and querying an HNSW index with FAISS",
    "file": "listing_5_4_building_and_querying_an_hnsw_index_with_faiss.py",
    "source_lines": [
      209,
      230
    ],
    "compiles": true,
    "provides": [
      "build_index",
      "faiss",
      "np",
      "query_index"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Self-contained: the listing imports faiss and numpy itself and only defines build_index/query_index. faiss-cpu is installed in .venv, so it execs clean. Verified pass in the harness."
  },
  {
    "number": "5.5",
    "title": "Spot check, query the index with Toy Story's embedding",
    "file": "listing_5_5_spot_check_query_the_index_with_toy_story_s_embedding.py",
    "source_lines": [
      246,
      249
    ],
    "compiles": true,
    "provides": [
      "query",
      "toy_story_idx"
    ],
    "category": "needs-real-data",
    "executable": false,
    "expected": "skip",
    "notes": "Runtime snippet referencing undefined prior symbols (movie_to_idx, toy_story_id, item_embeddings, hnsw_index, idx_to_title, query_index). Needs the trained embeddings + FAISS index built earlier in the chapter."
  },
  {
    "number": "5.6",
    "title": "Warm-starting a new item's embedding",
    "file": "listing_5_6_warm_starting_a_new_item_s_embedding.py",
    "source_lines": [
      287,
      314
    ],
    "compiles": true,
    "provides": [
      "new_embedding",
      "new_embedding_2d",
      "warm_start_embedding"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "Function annotations use `torch.Tensor`, evaluated at def time -> NameError without torch. DUPLICATE listing number 5.6 (second one; collides with 'Warm-starting a new item's embedding')."
  },
  {
    "number": "5.6",
    "title": "Hard negative mining",
    "file": "listing_5_6_hard_negative_mining.py",
    "source_lines": [
      357,
      382
    ],
    "compiles": true,
    "provides": [
      "hard_negative_mining"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "Function annotations use `torch.Tensor`, evaluated at def time -> NameError without torch. DUPLICATE listing number 5.6 (second one; collides with 'Warm-starting a new item's embedding')."
  },
  {
    "number": "5.7",
    "title": "InfoNCE loss",
    "file": "listing_5_7_infonce_loss.py",
    "source_lines": [
      411,
      440
    ],
    "compiles": true,
    "provides": [
      "InfoNCELoss"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "`class InfoNCELoss(torch.nn.Module)` needs torch at def; body also uses `F` (torch.nn.functional) which the listing never imports. Runs only in a torch session."
  },
  {
    "number": "5.8",
    "title": "A simple cross-encoder reranker",
    "file": "listing_5_8_a_simple_cross_encoder_reranker.py",
    "source_lines": [
      531,
      554
    ],
    "compiles": true,
    "provides": [
      "CrossEncoderReranker"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "`class CrossEncoderReranker(nn.Module)` needs torch (`nn`) at class-definition time -> NameError in the harness."
  },
  {
    "number": "5.9",
    "title": "Transformer cross-encoder reranker",
    "file": "listing_5_9_transformer_cross_encoder_reranker.py",
    "source_lines": [
      616,
      650
    ],
    "compiles": true,
    "provides": [
      "AutoModel",
      "AutoTokenizer",
      "TransformerCrossEncoder"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "Imports transformers and subclasses nn.Module; needs torch + transformers + network access to download bert-base-uncased. Not runnable in the harness."
  },
  {
    "number": "5.10",
    "title": "FourStageRecommender",
    "file": "listing_5_10_fourstagerecommender.py",
    "source_lines": [
      694,
      714
    ],
    "compiles": true,
    "provides": [
      "FourStageRecommender"
    ],
    "category": "api-drift",
    "executable": false,
    "expected": "skip",
    "notes": "NameError at def (annotations `Filtering`/`Scoring`/`RecommendationContext` undefined). BOOK<->PACKAGE DRIFT vs shipped recsys.fourstage_recsys.pipeline.FourStageRecommender: (1) constructor param `ranker` + call `self.ranker.rank(candidates)` vs package `ordering` + `ordering.order(candidates, context, debug)`; (2) `context.seed_movie_id` vs package `context.seed_items`; (3) `scorer.score(candidates)` (1 arg) vs package `scorer.score(candidates, context)` \u2014 and vs this chapter's own L5.12 `score(candidates, user_id)` (2 args); (4) base class `Scoring` vs package `Scorer`. Note: the book's `filter.filter(candidates, context.user_id)` here actually matches HistoryFiltering.filter's real `user_id` signature \u2014 more correct than the shipped pipeline which passes `context`."
  },
  {
    "number": "5.11",
    "title": "ANN-backed retrieval stage",
    "file": "listing_5_11_ann_backed_retrieval_stage.py",
    "source_lines": [
      728,
      757
    ],
    "compiles": true,
    "provides": [
      "ANNRetrieval"
    ],
    "category": "needs-package",
    "executable": false,
    "expected": "skip",
    "notes": "NameError at def (annotations `faiss.Index`/`np.ndarray` undefined in the harness). Chapter-local class; no ANNRetrieval exists in the recsys package. Defines `retrieve_similar_items(seed_movie_id, k)`, matching the book's own L5.10 pipeline (which calls a single seed id) rather than the package Retrieval ABC `retrieve(context)`."
  },
  {
    "number": "5.12",
    "title": "Cross-encoder scoring stage",
    "file": "listing_5_12_cross_encoder_scoring_stage.py",
    "source_lines": [
      777,
      800
    ],
    "compiles": true,
    "provides": [
      "CrossEncoderScoring"
    ],
    "category": "api-drift",
    "executable": false,
    "expected": "skip",
    "notes": "NameError at def: subclasses `Scoring`, which does not exist. BOOK<->PACKAGE DRIFT: the shipped scoring ABC is `recsys.fourstage_recsys.stages.scoring.Scorer` (not `Scoring`), and its method is `score(candidates, context)`; this listing declares `score(candidates, user_id)`. No CrossEncoderScoring class exists in the package."
  },
  {
    "number": "5.13",
    "title": "Transformer cross-encoder scoring stage",
    "file": "listing_5_13_transformer_cross_encoder_scoring_stage.py",
    "source_lines": [
      816,
      838
    ],
    "compiles": true,
    "provides": [
      "TransformerScoring"
    ],
    "category": "api-drift",
    "executable": false,
    "expected": "skip",
    "notes": "NameError at def: subclasses `Scoring` (package ABC is `Scorer`). Method `score(candidates, user_id)` drifts from package `score(candidates, context)`. No TransformerScoring class exists in the package."
  },
  {
    "number": "5.14",
    "title": "Assembling the upgraded pipeline",
    "file": "listing_5_14_assembling_the_upgraded_pipeline.py",
    "source_lines": [
      852,
      878
    ],
    "compiles": true,
    "provides": [
      "recommender",
      "recs",
      "retrieval",
      "scorer"
    ],
    "category": "api-drift",
    "executable": false,
    "expected": "skip",
    "notes": "NameError (ANNRetrieval, hnsw_index, DeduplicationFilter, ScoreRanker, RecommendationContext all undefined). BOOK<->PACKAGE DRIFT: `DeduplicationFilter` and `ScoreRanker` do not exist in the package; `RecommendationContext(seed_movie_id=..., user_id=..., k=...)` uses `seed_movie_id`, but the real field is `seed_items: List[str]` (no `seed_movie_id`), and `user_id` is typed `Optional[str]` so `user_id='user_42'` is fine while an int would fail validation. FourStageRecommender is constructed with `ranker=` (package param is `ordering=`)."
  },
  {
    "number": "5.15",
    "title": "Retrieval-specific evaluation",
    "file": "listing_5_15_retrieval_specific_evaluation.py",
    "source_lines": [
      906,
      938
    ],
    "compiles": true,
    "provides": [
      "evaluate_retrieval"
    ],
    "category": "standalone",
    "executable": true,
    "expected": "pass",
    "notes": "Pure function definition (annotations are builtins; np/retrieval.id_to_item resolved only at call). Verified it execs clean in the harness namespace."
  }
]